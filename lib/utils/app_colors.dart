import 'package:flutter/material.dart';

class AppColors {
  // Pure Black Background (iOS style)
  static const Color background = Colors.black;
  
  // Secondary Background (Cards, Search Bar)
  static const Color secondaryBackground = Color(0xFF1C1C1E);
  
  // Mid-light Orange (iOS Reminder Theme)
  static const Color themeColor = Color(0xFFFF9F0A);
  
  // Text Colors
  static const Color primaryText = Colors.white;
  static const Color secondaryText = Color(0xFF8E8E93);
  
  // Icon Colors
  static const Color iconColor = Colors.white;
  
  // Status Colors
  static const Color blue = Color(0xFF0A84FF);
  static const Color red = Color(0xFFFF453A);
  static const Color gray = Color(0xFF3A3A3C);
}
